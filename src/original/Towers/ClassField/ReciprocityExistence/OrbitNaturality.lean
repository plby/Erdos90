import Towers.ClassField.ReciprocityExistence.FiniteOrbitUnits

/-!
# The finite-orbit coordinate is the canonical completion embedding

The construction is split into map, dense-agreement, and unit-transport
modules so each dependent completion calculation is checked opaquely before
the next one is elaborated.
-/
