import
  Towers.Group.Zassenhaus.FixedRestartRouting

/-!
# Fixed-packet generated structural restart routing

The fixed-packet structural restart interface and its outer-residual scheduler
adapter now live together in
`SymbolicHallPowerTransientReachableFixedPacketGeneratedStructuralRestartOuterResidualRouting`.
This compatibility import keeps the earlier standalone module path available.

This file is intentionally not imported by the existing collection proof.
-/
